# 📖# Руководство пользователя tlg_keywords_follower- Telegram UserBot

Пошаговое руководство по настройке и использованию бота для автоматической пересылки сообщений.

---

## 🎯 Что делает этот бот?

Бот **мониторит** ваши каналы/группы и **автоматически пересылает** сообщения с определёнными ключевыми словами в нужные вам чаты.

**Пример:**

- У вас есть канал "Новости города"
- Вы хотите чтобы все сообщения со словом "срочно" автоматически пересылались в вашу личную группу
- Бот это сделает автоматически!

---

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Запустите бота

```bash
cd tlg_keywords_follower
python -m app.main
```

При первом запуске введите код из Telegram.

### Шаг 2: Добавьте канал/группу для мониторинга

**В Telegram:**

1. Перешлите **любое сообщение** из канала который хотите мониторить в **Saved Messages** (Избранное)
2. Ответьте на это сообщение командой: `/add_chat`
3. Бот ответит примерно так:

```text
✅ Чат добавлен в мониторинг!

📝 Название: Новости города
🆔 Chat ID: -1001234567890

💡 Теперь скопируйте этот ID в rules.txt
```

1. **Скопируйте** этот chat_id (например: `-1001234567890`)

### Шаг 3: Создайте правило пересылки

Создайте `app_data/rules.txt` из примера:

```bash
cp rules.example.txt app_data/rules.txt
```

Откройте файл `app_data/rules.txt` и добавьте строку:

```text
срочные_новости: срочно, важно, breaking -> -1001234567890 "Новости города"
```

**Формат:**

```
название_правила: ключевые_слова -> chat_id "Название чата"
```

### Шаг 4: Перезагрузите конфигурацию

Отправьте в Saved Messages:

```
/reload
```

Бот ответит:

```
✅ Конфигурация перезагружена

📊 Правил: 1
👁 Мониторимых чатов: 1
```

### Шаг 5: Готово! 🎉

Теперь все сообщения из "Новости города" со словами "срочно", "важно" или "breaking" будут автоматически пересылаться!

---

## 📋 Подробная инструкция

### Как работает бот?

```
┌─────────────────────────┐
│ Ваши каналы/группы      │  ← Бот мониторит
│ (monitored_chats)       │
└──────────┬──────────────┘
           │
           │ Новое сообщение: "СРОЧНО: авария"
           │
           ▼
┌─────────────────────────┐
│ Проверка по правилам    │  ← Есть слово "срочно"?
│ (rules.txt)             │
└──────────┬──────────────┘
           │
           │ Да! Правило сработало
           │
           ▼
┌─────────────────────────┐
│ Пересылка в целевой чат │  ← Автоматическая отправка
│ (target_chat_ids)       │
└─────────────────────────┘
```

---

## 🔧 Настройка правил (`app_data/rules.txt`)

**⚠️ ВАЖНО:** Правила срабатывают ТОЛЬКО для сообщений из чатов, которые находятся в списке **Мониторимые чаты**.
Если вы создали правило, но не добавили чат-источник через `/add_chat` (или добавив бота) — пересылка работать не будет!

### Формат файла

```text
название: ключевые_слова -> chat_id "Название группы" [case:on]
```

- **название**: Уникальное имя правила (латиница, без пробелов).
- **ключевые_слова**: Список слов через запятую.
- **chat_id**: ID чата куда пересылать (начинается с `-100...` для супергрупп).
- **"Название группы"**: Опциональное описание для удобства (в кавычках).
- **[case:on]**: Опциональный флаг для учёта регистра.

### Примеры правил

#### 1. Простое правило

```text
новости: новость, news -> -1001234567890 "Моя группа"
```

Пересылает сообщения с "новость" или "news" в чат -1001234567890.

#### 2. Несколько целевых чатов

```text
срочное: срочно, urgent -> -1001111111111 "Главная", -1002222222222 "Резервная"
```

Сообщение будет отправлено **во все** указанные чаты.

#### 3. Учёт регистра ([case:on])

```text
admin: @Admin -> -1003333333333 "Админы" [case:on]
```

Сработает только на `@Admin`, но не на `@admin` или `@admin`.
Без этого флага регистр не важен ("Срочно" == "срочно").

#### 4. Несколько правил на одно сообщение

```text
срочное: срочно -> -1001111 "Срочные"
общее: новость -> -1002222 "Все новости"
```

Если сообщение содержит "срочно новость", оно попадёт и в "Срочные", и в "Все новости".

### Комментарии

Используйте `#` для комментариев. Бот их игнорирует.

```text
# Это комментарий
breaking: срочно -> -1001234
# trading: buy -> -1005678  <- Это правило выключено
```

### Пример полного rules.txt

```text
# Правила пересылки сообщений

# Срочные сообщения → в главную группу и личный чат
urgent: срочно, важно, внимание, breaking -> -1001234567890 "Важные уведомления", -1009876543210 "Личный чат"

# Торговые сигналы → в группу трейдеров
trading: deal, price, buy, sell, сделка -> -1005555555555 "Трейдинг сигналы"

# Админские уведомления (с учётом регистра)
admin: @Admin, @admin -> -1007777777777 "Админская группа" [case:on]

# Группа без названия (будет "Unknown" при загрузке)
test: тест -> -1001111111111
```

---

## 💬 Справочник команд

Все команды отправляются боту в **Saved Messages (Избранное)**.
Бот автоматически синхронизирует изменения с `rules.txt` файлом.

### � Просмотр и информация

#### `/rules` или `/list`

Показывает все активные правила с их настройками.

#### `/rule <название>`

Показывает детали конкретного правила.
Пример: `/rule urgent`

#### `/test <текст>`

Проверяет какие правила сработают для заданного текста.
Пример: `/test Срочная новость`

### ➕ Создание и редактирование правил

#### `/create_rule <название>`

Создаёт новое пустое правило.
Пример: `/create_rule news`

#### `/add_rule <название> <ключевые_слова> -> <чаты>`

Быстрое создание правила.
Пример: `/add_rule urgent срочно,важно -> -1001234`

#### `/add_keywords <правило> <слова>`

Добавляет ключевые слова (через запятую).
Пример: `/add_keywords urgent внимание,alert`

#### `/add_group <правило> <chat_id>`

Добавляет целевой чат. Можно переслать сообщение из группы и ответить этой командой.
Пример: `/add_group urgent -1001234`

#### `/set_case <правило> <on|off>`

Включает/выключает учёт регистра.

### ➖ Удаление

#### `/remove_rule <название>`

Удаляет правило полностью. Требует подтверждения.

#### `/remove_keywords <правило> <слова>`

Удаляет конкретные ключевые слова.

#### `/remove_group <правило> <chat_id>`

Удаляет целевой чат из правила.

### 🔄 Системные команды

#### `/reload`

Перезагружает конфигурацию с диска (полезно после ручной правки `rules.txt`).

#### `/save`

Принудительно сохраняет текущие правила в файл.

#### `/backup`

Создаёт резервную копию `rules.txt`.

#### `/validate`

Проверяет корректность всех правил.

#### `/clean_rule <название>`

Удаляет дубликаты слов и чатов в правиле.

### 📊 Статистика и настройки

#### `/stats`

Показывает статистику работы бота (количество срабатываний, топ правил).

#### `/settings`

Показывает текущие настройки (пути файлов, режим пересылки).

### ⚡ Быстрые команды (алиасы)

- `/r` = `/rules`
- `/t` = `/test`
- `/ak` = `/add_keywords`
- `/ag` = `/add_group`
- `/rk` = `/remove_keywords`
- `/rg` = `/remove_group`

## 🎓 Примеры использования

### Пример 1: Мониторинг новостного канала

**Задача:** Все срочные новости из канала "Новости города" отправлять в личную группу.

**Шаги:**

1. Перешлите сообщение из "Новости города" в Saved Messages
2. `/add_chat` → получите chat_id (например `-1001111111111`)
3. Создайте целевую группу (или используйте существующую)
4. Получите её chat_id тем же способом (например `-1002222222222`)
5. Отредактируйте `rules.txt`:

```
срочные_новости: срочно, важно, breaking -> -1002222222222 "Моя группа"
```

1. `/reload`

**Готово!** Теперь все сообщения со словами "срочно", "важно", "breaking" из -1001111111111 будут автоматически отправляться в -1002222222222.

### Пример 2: Несколько источников → одна группа

**Задача:** Мониторить 3 новостных канала, отправлять срочное в одну группу.

**Шаги:**

1. Добавьте все 3 канала через `/add_chat`
2. Они все появятся в `monitored_chats`
3. Создайте правило:

```
все_срочное: срочно, urgent, breaking -> -1009999999999 "Срочные новости"
```

1. `/reload`

Теперь срочные сообщения из **всех трёх** каналов будут в одной группе!

### Пример 3: Один источник → разные группы

**Задача:** Из канала "Крипто новости" отправлять:

- Сообщения про Bitcoin → в группу "BTC трейдеры"
- Сообщения про Ethereum → в группу "ETH трейдеры"
- Срочные сообщения → в обе группы

**rules.txt:**

```
bitcoin: bitcoin, btc, биткоин -> -1001111 "BTC трейдеры"
ethereum: ethereum, eth, эфир -> -1002222 "ETH трейдеры"
срочное: срочно, alert, важно -> -1001111 "BTC трейдеры", -1002222 "ETH трейдеры"
```

**Результат:**

- "Биткоин вырос на 10%" → только в "BTC трейдеры"
- "Ethereum достиг нового максимума" → только в "ETH трейдеры"
- "СРОЧНО: биткоин упал" → в **ОБЕ** группы (сработали 2 правила)

### Пример 4: Фильтрация спама

**Задача:** Из канала пересылать всё КРОМЕ рекламы.

**Решение:** Создайте правила только для нужных ключевых слов:

```
полезное: обзор, анализ, новость, прогноз -> -1001111 "Без спама"
```

Реклама (обычно со словами "купить", "скидка" и т.д.) в правила не попадает → не пересылается.

---

## 🔍 Получение chat_id

### Способ 1: Через `/add_chat` (рекомендуется)

1. Перешлите сообщение из нужного чата в Saved Messages
2. Ответьте `/add_chat`
3. Скопируйте chat_id из ответа

### Способ 2: Через бота (для групп)

1. Добавьте UserBot в группу
2. ID автоматически добавится в `monitored_chats`
3. `/monitored_chats` → скопируйте ID

### Способ 3: Через @userinfobot

1. Добавьте @userinfobot в группу
2. Напишите `/start`
3. Бот покажет ID

---

## ⚙️ Настройки (app_data/config.json)

```json
{
  "monitored_chats": [...],        // Не трогайте - управляется автоматически
  "rules": [],                      // Не трогайте - используйте rules.txt
  "forward_mode": "copy",           // "copy" или "forward"
  "auto_add_chats": true            // true или false
}
```

### forward_mode

- **`"copy"`** (по умолчанию) - копирует только текст сообщения
- **`"forward"`** - пересылает с указанием автора и ссылкой на оригинал

### auto_add_chats

- **`true`** (по умолчанию) - при добавлении бота в группу, она автоматически добавляется в мониторинг
- **`false`** - нужно добавлять вручную через `/add_chat`

**Примечание:** Для каналов НЕ работает, используйте `/add_chat`.

---

## ❓ FAQ (Часто задаваемые вопросы)

### Бот не видит сообщения из канала

**Причина:** Канал не добавлен в `monitored_chats`.

**Решение:** Используйте `/add_chat` с пересылкой сообщения из канала.

### Правило не срабатывает

**Проверьте:**

1. Запустили `/reload` после редактирования rules.txt?
2. Правильный формат строки?
3. Используйте `/test <текст>` для проверки

**Пример проверки:**

```
/test Срочная новость
```

Если правило не срабатывает - проверьте написание ключевых слов.

### Сообщение пересылается несколько раз в один чат

**Причина:** Несколько правил указывают на один chat_id.

**Решение:** Это нормально! Бот автоматически дедуплицирует - отправит только один раз.

### FloodWaitError в логах

**Что это:** Telegram ограничивает количество сообщений в секунду.

**Что делает бот:** Автоматически обрабатывает - сообщения сохраняются в очередь и отправляются с задержкой.

**Действия:** Ничего делать не нужно, бот справится сам.

### Как остановить бота?

Нажмите `Ctrl+C` в терминале.

### Как удалить правило?

1. Откройте `app_data/rules.txt`
2. Удалите строку с правилом или закомментируйте её (`#`)
3. `/reload`

### Как удалить чат из мониторинга?

1. Откройте `app_data/config.json`
2. Удалите объект из `monitored_chats`
3. Перезапустите бота

---

## 🔐 Безопасность

### ⚠️ Важно знать

1. **Session файл** (`*.session`) содержит полный доступ к вашему аккаунту
   - Никогда не передавайте его другим
   - Не коммитьте в Git

2. **`.env` файл** содержит API ключи
   - Храните в безопасности
   - Не коммитьте в Git

3. **Целевые чаты vs Мониторимые**
   - Бот автоматически не будет мониторить чаты, куда сам пересылает
   - Это защита от зацикливания

### chmod 600 для чувствительных файлов

```bash
chmod 600 .env
chmod 600 *.session
```

---

## 📊 Мониторинг работы

### Просмотр логов

```bash
tail -f bot.log
```

### Полезная информация в логах

```
✨ Сообщение совпало с правилами: ['срочное'] (ключевые слова: ['срочно'])
Сообщение добавлено в очередь: правила=['срочное'], чатов=1
Сообщение отправлено в -1001111111111
```

### Ротация логов

Логи автоматически ротируются:

- Максимальный размер: 10 MB
- Хранится: 5 файлов (bot.log, bot.log.1, ..., bot.log.5)

---

## 🚨 Troubleshooting

### Ошибка: "API_ID not found"

**Решение:** Создайте файл `.env` из `.env.example` и заполните данными.

### Ошибка при парсинге rules.txt

**Проверьте формат:**

```
✅ Правильно:
urgent: срочно, важно -> -1001234 "Группа"

❌ Неправильно:
urgent срочно важно -1001234         # Нет : и ->
urgent: срочно важно -> -1001234     # Нет запятой между словами
```

### Бот не отвечает на команды

**Проверьте:**

1. Команды отправляются в **Saved Messages**?
2. Команда начинается с **/**?
3. Бот запущен?

### Очередь растёт (много файлов в queue/)

**Причина:** FloodWaitError или ошибки отправки.

**Решение:**

- Проверьте логи: `tail -f bot.log`
- Проверьте что целевые чаты существуют
- Подождите - бот обработает очередь автоматически

---

## 📞 Поддержка

### Документация

- `README.md` - установка и быстрый старт
- `USER_GUIDE.md` - это руководство и справочник команд

### Логи

Всегда проверяйте логи при проблемах:

```bash
tail -f bot.log
```

---

## ✅ Чек-лист для новичков

- [ ] Установил Python 3.11+
- [ ] Создал виртуальное окружение
- [ ] Установил зависимости (`pip install -r requirements.txt`)
- [ ] (для тестов) Установил `pip install -r requirements.dev.txt`
- [ ] Получил API credentials на <https://my.telegram.org>
- [ ] Создал `.env` файл с credentials
- [ ] Запустил бота (`python main.py`)
- [ ] Авторизовался (ввёл код из Telegram)
- [ ] Добавил первый канал через `/add_chat`
- [ ] Создал первое правило в `rules.txt`
- [ ] Выполнил `/reload`
- [ ] Протестировал через `/test`
- [ ] Всё работает! 🎉

---

**Удачи в использовании бота!** 🚀

Если возникнут вопросы - проверьте FAQ или логи (`bot.log`).
