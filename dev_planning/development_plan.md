# План разработки Telegram UserBot для мониторинга и пересылки сообщений

## Описание проекта

Минималистичный Telegram UserBot, работающий под учётными данными пользователя, который мониторит указанные чаты и пересылает сообщения с заданными ключевыми словами в одну или несколько целевых групп.

## Архитектура решения

### Упрощённая архитектура

```
┌─────────────────────────────────────────┐
│          Telegram API                   │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│    Python скрипт (bot.py)               │
│  ┌────────────────────────────────────┐ │
│  │  Загрузка config.json              │ │
│  └────────────┬───────────────────────┘ │
│  ┌────────────▼───────────────────────┐ │
│  │  Мониторинг чатов из списка        │ │
│  └────────────┬───────────────────────┘ │
│  ┌────────────▼───────────────────────┐ │
│  │  Фильтрация по ключевым словам     │ │
│  └────────────┬───────────────────────┘ │
│  ┌────────────▼───────────────────────┐ │
│  │  Пересылка в целевые группы (1-N)  │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│   systemd сервис на Linux сервере       │
└─────────────────────────────────────────┘
```

## Технологический стек

### Основные технологии

- **Python 3.11+** - основной язык разработки
- **Telethon** - библиотека для работы с Telegram MTProto API
- **systemd** - управление сервисом
- **JSON** - хранение конфигурации
- **Linux** - целевая платформа

### Библиотеки (минимум)

- **Telethon** - работа с Telegram API
- **python-dotenv** - управление credentials (.env файл)

---

## Этапы разработки

### Этап 1: Подготовка и проектирование (1 день)

#### 1.1. Анализ требований

- [ ] Определить список мониторимых чатов (chat_id)
- [ ] Определить **правила фильтрации** - группы ключевых слов с привязкой к целевым чатам
- [ ] Для каждого правила указать:
  - Список ключевых слов
  - Целевые группы для пересылки (можно несколько)
  - Учитывать ли регистр (case_sensitive)
- [ ] Решить вопросы с форматированием пересылаемых сообщений

> [!TIP]
> Гибкость правил позволяет одно сообщение пересылать в разные группы в зависимости от содержания. Например: сообщение "СРОЧНО: новая цена" попадёт под правила и "urgent_messages" (срочно) и "trading_signals" (цена) и будет отправлено во все указанные в этих правилах группы.

#### 1.2. Структура конфигурации (config.json)

**Полный пример с пояснениями:**

```json
{
  "monitored_chats": [
    {
      "id": -1001111111111,
      "name": "Новостной канал"
    },
    {
      "id": -1002222222222,
      "name": "Трейдинг сигналы"
    },
    {
      "id": -1003333333333,
      "name": "Аналитика и прогнозы"
    }
  ],
  "rules": [
    {
      "name": "urgent_messages",
      "keywords": ["важно", "срочно", "внимание"],
      "target_chat_ids": [-1001234567890, -1009999999999],
      "case_sensitive": false
    },
    {
      "name": "trading_signals",
      "keywords": ["deal", "price", "buy", "sell"],
      "target_chat_ids": [-1005555555555],
      "case_sensitive": false
    },
    {
      "name": "admin_alerts",
      "keywords": ["@admin", "помощь"],
      "target_chat_ids": [-1007777777777, -1008888888888],
      "case_sensitive": true
    }
  ],
  "forward_mode": "copy",
  "auto_add_chats": true
}
```

---

### 📝 Описание полей

**`monitored_chats`** - список чатов, которые бот **МОНИТОРИТ** (откуда читает сообщения)

- Формат: массив объектов `{"id": chat_id, "name": "Название"}`
- Это источники сообщений
- Бот подписывается на новые сообщения из этих чатов
- При `auto_add_chats: true` - сюда автоматически добавляются чаты с их названиями
- **Название сохраняется один раз** при добавлении, не обновляется автоматически

**`rules`** - правила фильтрации и маршрутизации:

- `name` - имя правила (для логов)
- `keywords` - ключевые слова этого правила
- `target_chat_ids` - список чатов, **КУДА ПЕРЕСЫЛАТЬ** сообщения при совпадении
- `case_sensitive` - учитывать регистр для этого правила

**`forward_mode`** - режим пересылки (глобально для всех правил):

- `"forward"` - обычная пересылка (сохраняет автора и ссылку)
- `"copy"` - копирование текста с метаданными

**`auto_add_chats`** - автоматическое добавление чатов:

- `true` - при добавлении бота в новую группу → она добавляется в `monitored_chats`
- `false` - нужно вручную добавлять chat_id в `monitored_chats`

---

### 🔄 Как работает автоматическое добавление

```
1. Вы добавляете бота в группу "Новостной канал" (ID: -1004444444444)
   ↓
2. Бот получает событие ChatAction
   ↓
3. Получает информацию о чате через Telethon API:
   - ID: -1004444444444
   - Название: "Новостной канал"
   ↓
4. Проверяет: это НЕ один из target_chat_ids? ✅
   ↓
5. Добавляет объект {"id": -1004444444444, "name": "Новостной канал"} 
   в monitored_chats в config.json
   ↓
6. ТЕПЕРЬ бот мониторит эту группу и проверяет сообщения по правилам
```

---

### 💡 Примеры сценариев

#### Сценарий 1: Простая настройка

**У вас есть:**

- 3 новостных канала (источники)
- 1 группа для важных новостей (получатель)

**rules.txt:**

```
# Формат: название_правила: ключевые_слова -> chat_id "Название группы", chat_id2 "Название2"

urgent: срочно, важно, breaking -> -100999 "Важные новости"
```

**Что происходит:**

- Бот читает сообщения из мониторимых чатов
- Если в сообщении есть "важно", "срочно" или "breaking" → пересылает в -100999
- Название "Важные новости" - только для удобства, на работу не влияет
- Если добавить бота в новый канал → он автоматически добавится в `monitored_chats`

---

#### Сценарий 2: Разные темы → разные группы

**У вас есть:**

- 5 торговых каналов (источники)
- Группа для срочных сделок
- Группа для аналитики
- Группа для техподдержки

**rules.txt:**

```
# Срочные сделки
urgent_deals: срочно, быстро, сейчас -> -100777 "Срочные сделки"

# Аналитика
analytics: анализ, прогноз, стратегия -> -100888 "Аналитика"

# Техподдержка - в две группы
support: помощь, вопрос, @support -> -100999 "Техподдержка", -100555 "Админы"
```

**Что происходит:**

- Сообщение "Срочно нужна помощь!" → попадёт в "Техподдержка" и "Срочные сделки"
- Сообщение "Анализ рынка" → только в "Аналитика"
- Названия групп сохранены для удобства чтения конфигурации

---

> [!IMPORTANT]
> **Ключевое правило:**
>
> - `monitored_chats` = ОТКУДА читать (источники)
> - `target_chat_ids` = КУДА пересылать (получатели)
> - Автоматически добавляются только **источники** (monitored_chats)
> - **Получатели** (target_chat_ids) ВСЕГДА указываются вручную в правилах

> [!WARNING]
> **Защита от зацикливания:** Если один из `target_chat_ids` случайно окажется в `monitored_chats` (даже автоматически), бот удалит его оттуда при старте и залогирует предупреждение.

---

### Этап 2: Настройка проекта (0.5 дня)

#### 2.1. Модульная структура проекта

```
tlg_keywords_follower/
├── app/
│   ├── __init__.py
│   ├── main.py              # Точка входа, инициализация бота
│   ├── config_manager.py    # Управление config.json и rules.txt
│   ├── queue_manager.py     # Файловая очередь сообщений
│   ├── handlers.py          # Обработчики Telegram событий (ChatAction, NewMessage)
│   └── commands.py          # Команды управления правилами
├── config.json          # Конфигурация (общие настройки, monitored_chats)
├── rules.txt            # Правила фильтрации и пересылки
├── .env                 # API credentials (API_ID, API_HASH, PHONE)
├── requirements.txt     # Зависимости Python
├── Dockerfile
├── docker-compose.yml
├── deploy_docker.sh
├── .gitignore
├── queue/               # Директория для файловой очереди
│   └── .gitkeep
└── README.md            # Инструкция по установке
```

**Обоснование модульности:**

- ✅ Удобство поддержки - каждый модуль отвечает за свою область
- ✅ Читаемость - файлы по 100-200 строк вместо одного на 600+
- ✅ Тестируемость - легче тестировать отдельные модули
- ✅ Минимализм сохранён - всего 5 Python файлов

**Размеры модулей (примерно):**

- `main.py` - 50-80 строк (инициализация и запуск)
- `config_manager.py` - 150-200 строк (config.json, rules.txt, парсинг)
- `queue_manager.py` - 100-150 строк (очередь, worker, FloodWaitError)
- `handlers.py` - 100-150 строк (ChatAction, NewMessage)
- `commands.py` - 200-250 строк (все команды управления)

**Общий объём:** ~600-830 строк (как и планировалось, но в модулях)

#### 2.2. Инициализация проекта

- [ ] Создать директорию `tlg_keywords_follower`
- [ ] Создать виртуальное окружение Python: `python3 -m venv venv`
- [ ] Создать файл `requirements.txt`
- [ ] Настроить Git репозиторий (опционально)
- [ ] Создать `.gitignore` (исключить `.env`, `*.session`, `venv/`)

**requirements.txt:**

```txt
telethon==1.34.0
python-dotenv==1.0.0
```

**Содержимое .gitignore:**

```txt
# Credentials и session
.env
*.session
*.session-journal

# Файловая очередь
queue/*.json

# Python
venv/
__pycache__/
*.pyc
*.pyo

# IDE
.vscode/
.idea/
```

---

### Этап 3: Получение API credentials (1 день)

#### 3.1. Регистрация приложения

- [ ] Зайти на <https://my.telegram.org>
- [ ] Создать приложение (Apps)
- [ ] Получить `API_ID` и `API_HASH`
- [ ] Сохранить credentials в `.env`

#### 3.2. Авторизация userbot

- [ ] Запустить скрипт первичной авторизации
- [ ] Ввести номер телефона
- [ ] Ввести код подтверждения
- [ ] Ввести пароль 2FA (если есть)
- [ ] Получить session файл

> [!WARNING]
> Session файл содержит полный доступ к аккаунту! Хранить в безопасном месте и НЕ коммитить в Git!

---

### Этап 4: Разработка бота (2-3 дня)

#### 4.1. Модульная структура кода

Код разбит на **5 модулей** для удобства поддержки:

1. **`main.py`** (50-80 строк) - точка входа, инициализация
2. **`config_manager.py`** (150-200 строк) - управление config.json и rules.txt
3. **`queue_manager.py`** (100-150 строк) - файловая очередь, FloodWaitError
4. **`handlers.py`** (100-150 строк) - обработчики Telegram событий
5. **`commands.py`** (200-250 строк) - команды управления правилами

> [!TIP]
> Подробное описание каждого модуля с примерами кода находится в файле **`modules_structure.md`**

**Краткое описание модулей:**

##### main.py

```python
"""Точка входа приложения"""
import asyncio
import logging
from logging.handlers import RotatingFileHandler
from telethon import TelegramClient
from dotenv import load_dotenv
import os

from config_manager import ConfigManager
from queue_manager import QueueManager
from handlers import setup_handlers
from commands import setup_commands

async def main():
    """Главная функция запуска бота"""
    # Загрузка credentials
    load_dotenv()
    api_id = int(os.getenv('API_ID'))
    api_hash = os.getenv('API_HASH')
    phone = os.getenv('PHONE')
    
    # Инициализация менеджера конфигурации
    config_mgr = ConfigManager()
    config_mgr.load()
    
    # Инициализация клиента
    client = TelegramClient('userbot_session', api_id, api_hash)
    await client.start(phone=phone)
    
    # Инициализация очереди
    queue_mgr = QueueManager(config_mgr)
    
    # Регистрация обработчиков
    setup_handlers(client, config_mgr, queue_mgr)
    setup_commands(client, config_mgr)
    
    # Запуск queue worker
    asyncio.create_task(queue_mgr.process_queue(client))
    
    # Запуск бота
    await client.run_until_disconnected()

if __name__ == '__main__':
    # Настройка логирования с ротацией
    logging.basicConfig(
        level=logging.INFO,
        format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
        handlers=[
            RotatingFileHandler('bot.log', maxBytes=10*1024*1024, backupCount=5),
            logging.StreamHandler()
        ]
    )
    
    asyncio.run(main())
```

##### config_manager.py

- Загрузка/сохранение `config.json`
- Парсинг `rules.txt`
- Валидация конфигурации
- API для доступа к настройкам
- Методы добавления/удаления правил и чатов

##### queue_manager.py

- `add_to_queue()` - добавление в очередь
- `process_queue()` - фоновый worker
- `send_message_safe()` - отправка с обработкой FloodWaitError
- Управление JSON файлами в `queue/`

##### handlers.py

- `@ChatAction` - автодобавление чатов
- `@NewMessage` - фильтрация по правилам
- `check_message_against_rules()` - проверка правил
- `get_unique_target_chats()` - дедупликация

##### commands.py

- Роутинг команд
- `/rules`, `/rule`, `/test` - просмотр
- `/add_rule`, `/add_keywords`, `/add_group` - добавление
- `/remove_rule`, `/remove_keywords`, `/remove_group` - удаление
- `/reload`, `/save`, `/backup`, `/validate` - системные

#### 4.2. Чек-лист разработки по модулям

> [!NOTE]
> Каждый модуль разрабатывается отдельно. Детальные примеры кода в `modules_structure.md`

##### Модуль 1: config_manager.py

- [ ] **Класс ConfigManager:**
  - `__init__()` - инициализация путей
  - `load()` - загрузка config.json и rules.txt
  - `save()` - сохранение обоих файлов
  - `validate_and_clean()` - валидация monitored_chats

- [ ] **Загрузка config.json:**
  - Чтение JSON файла
  - Создание default config при отсутствии
  - Валидация структуры

- [ ] **Парсинг rules.txt:**
  - `_parse_rule_line()` - парсинг строки формата `name: keywords -> chat_id "Name" [case:on]`
  - Извлечение названий групп
  - Обработка опции `[case:on]`
  - Валидация синтаксиса

- [ ] **Методы управления:**
  - `add_monitored_chat(chat_id, name)` - добавление чата
  - `get_monitored_chat_ids()` - список ID
  - `get_rules()` - получение правил
  - API для команд (add_rule, remove_rule, etc.)

##### Модуль 2: queue_manager.py

- [ ] **Класс QueueManager:**
  - `__init__(config_manager, queue_dir='queue')`
  - Создание директории queue/

- [ ] **Управление очередью:**
  - `add_to_queue(message_data)` - сохранение в JSON
  - `get_queue_items()` - список файлов (отсортированный)
  - `remove_from_queue(filename)` - удаление после отправки

- [ ] **Отправка сообщений:**
  - `send_message_safe()` - с обработкой FloodWaitError
  - Поддержка forward mode (message_id + from_chat_id)
  - Поддержка copy mode (только текст)
  - Возврат True/False для retry логики

- [ ] **Queue Worker:**
  - `process_queue()` - асинхронный бесконечный цикл
  - Чтение JSON файлов
  - Отправка во все target_chat_ids
  - Retry при FloodWaitError (60 сек)
  - Проверка очереди каждые 5 сек

##### Модуль 3: handlers.py

- [ ] **Функция setup_handlers(client, config_mgr, queue_mgr):**
  - Регистрация всех обработчиков

- [ ] **ChatAction обработчик:**
  - `@client.on(events.ChatAction)`
  - Проверка `event.user_added and event.is_self`
  - Получение chat info через `event.get_chat()`
  - Проверка что не target_chat_id
  - Добавление в monitored_chats с названием
  - Сохранение config.json

- [ ] **NewMessage обработчик:**
  - `@client.on(events.NewMessage(chats=monitored_ids))`
  - Получение текста сообщения
  - Проверка по правилам
  - Дедупликация target_chat_ids
  - Добавление в очередь

- [ ] **Вспомогательные функции:**
  - `check_message_against_rules(text, rules)` - возвращает matched_rules
  - `get_unique_target_chats(matched_rules)` - дедупликация по ID

##### Модуль 4: commands.py

- [ ] **Функция setup_commands(client, config_mgr):**
  - Регистрация обработчика команд

- [ ] **Роутер команд:**
  - `@client.on(events.NewMessage(from_users='me', pattern=r'^/\w+'))`
  - Парсинг команды и аргументов
  - Маршрутизация к функциям

- [ ] **Команды просмотра:**
  - `cmd_list_rules()` - /rules, /list
  - `cmd_show_rule(rule_name)` - /rule <name>
  - `cmd_test_message(text)` - /test <text>
  - `cmd_stats()` - статистика

- [ ] **Команды создания/изменения:**
  - `cmd_create_rule()` - /create_rule
  - `cmd_add_rule()` - /add_rule
  - `cmd_add_keywords()` - /add_keywords
  - `cmd_add_group()` - /add_group (с пересылкой)
  - `cmd_set_case()` - /set_case

- [ ] **Команды удаления:**
  - `cmd_remove_rule()` - /remove_rule (с подтверждением)
  - `cmd_remove_keywords()` - /remove_keywords
  - `cmd_remove_group()` - /remove_group

- [ ] **Системные команды:**
  - `cmd_reload()` - /reload
  - `cmd_save()` - /save
  - `cmd_backup()` - /backup
  - `cmd_validate()` - /validate
  - `cmd_monitored_chats()` - /monitored_chats
  - `cmd_help()` - /help

##### Модуль 5: main.py

- [ ] **Настройка логирования:**
  - RotatingFileHandler (10MB, 5 файлов)
  - StreamHandler для консоли
  - Формат с timestamp

- [ ] **Загрузка credentials:**
  - `load_dotenv()`
  - Чтение API_ID, API_HASH, PHONE

- [ ] **Инициализация:**
  - Создание ConfigManager
  - Создание QueueManager
  - Создание TelegramClient
  - Авторизация

- [ ] **Запуск:**
  - `setup_handlers()`
  - `setup_commands()`
  - `asyncio.create_task(queue_worker)`
  - `client.run_until_disconnected()`

- [ ] **Graceful shutdown:**
  - Обработка KeyboardInterrupt
  - Корректное закрытие клиента

#### 4.3. Пример .env файла

```env
API_ID=12345678
API_HASH=abcdef1234567890abcdef1234567890
PHONE=+79991234567
```

---

### Этап 5: Тестирование (1-2 дня)

#### 5.1. Локальное тестирование

- [ ] Создать тестовый `config.json` с несколькими правилами и `auto_add_chats: true`
- [ ] Запустить бот: `python bot.py`
- [ ] Проверить авторизацию (первый запуск)
- [ ] Проверить что создалась папка `queue/`
- [ ] **Тест автодобавления:** Добавить бота в тестовую группу
- [ ] Проверить что chat_id автоматически добавился в `monitored_chats`
- [ ] Проверить логи (должно быть сообщение о добавлении чата)
- [ ] **Тест одного правила:** Отправить сообщение с ключевым словом из первого правила
- [ ] Проверить что сообщение попало только в target_chat_ids первого правила
- [ ] **Тест нескольких правил:** Отправить сообщение содержащее ключевые слова из РАЗНЫХ правил
- [ ] Проверить что сообщение попало во ВСЕ target_chat_ids всех сработавших правил
- [ ] Проверить логи - должны быть указаны все сработавшие правила
- [ ] **Тест дедупликации:** Если разные правила указывают на один и тот же target_chat_id
- [ ] Убедиться что в этот чат сообщение отправлено только ОДИН раз
- [ ] **Проверить очередь:** Убедиться что создался JSON файл в `queue/`
- [ ] Проверить содержимое JSON файла (список target_chat_ids, текст, имена правил)
- [ ] Убедиться что queue worker обработал файл (файл удалён)
- [ ] **Тест case_sensitive:** Отправить сообщение с разным регистром
- [ ] Убедиться что правило с `case_sensitive: true` срабатывает только при точном совпадении
- [ ] Убедиться что правило с `case_sensitive: false` срабатывает независимо от регистра
- [ ] **Тест FloodWaitError (симуляция):**
  - Отправить несколько сообщений быстро
  - Проверить что при FloodWaitError файлы остаются в очереди
  - Проверить логи - должно быть сообщение о времени ожидания
  - Убедиться что worker повторяет попытку отправки
- [ ] **Тест защиты от зацикливания:** Попробовать добавить любой target_chat_id (из любого правила) в monitored_chats вручную
- [ ] Убедиться что он игнорируется (проверить логи-предупреждения)
- [ ] Тест остановки (Ctrl+C) и корректного завершения
- [ ] **Проверка персистентности очереди:**
  - Остановить бота с файлами в очереди
  - Запустить снова
  - Убедиться что необработанные сообщения отправлены

#### 5.2. Автоматическое получение chat_id

**Бот теперь делает это сам!**

Когда вы добавляете бота в группу или канал:

1. Бот автоматически получает `chat_id` **и название**
2. Проверяет что это не один из `target_chat_ids`
3. Добавляет объект `{"id": chat_id, "name": "Название"}` в `monitored_chats` в `config.json`
4. Логирует событие

**Просмотр мониторимых чатов:**

```
/monitored_chats

Ответ:
👁 Мониторимые чаты (3):

1. -1001111111111 - "Новостной канал"
2. -1002222222222 - "Трейдинг сигналы"  
3. -1003333333333 - "Аналитика и прогнозы"

💡 Для использования в правилах копируйте chat_id
```

**Ручное получение (опционально):**

```bash
# Если нужно узнать chat_id вручную:
# 1. Добавить @userinfobot в чат
# 2. Написать /start
# 3. Бот покажет ID чата
```

---

### Этап 6: Деплой на сервер (1 день)

#### 6.1. Подготовка сервера

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python 3.11+
sudo apt install python3 python3-pip python3-venv -y

# Создание пользователя для бота (опционально, но рекомендуется)
sudo useradd -m -s /bin/bash tlgbot
sudo su - tlgbot
```

#### 6.2. Развёртывание бота

```bash
# Создание директории проекта
mkdir ~/tlg_keywords_follower
cd ~/tlg_keywords_follower

# Копирование файлов (через git или scp)
# Вариант 1: Git
git clone <your-repo-url> .

# Вариант 2: SCP с локальной машины
# scp -r /path/to/tlg_keywords_follower/* user@server:~/tlg_keywords_follower/

# Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Настройка конфигурации
nano .env          # Заполнить API_ID, API_HASH, PHONE
nano config.json   # Заполнить чаты и ключевые слова

# Первичная авторизация (ВАЖНО: интерактивно!)
python bot.py
# Ввести код из Telegram
# После успешной авторизации - Ctrl+C
```

#### 6.3. Настройка systemd сервиса

**Создание unit файла:**

```bash
sudo nano /etc/systemd/system/tlgbot.service
```

**Содержимое tlgbot.service:**

```ini
[Unit]
Description=Telegram UserBot - Message Forwarder
After=network.target

[Service]
Type=simple
User=tlgbot
WorkingDirectory=/home/tlgbot/tlg_keywords_follower
Environment="PATH=/home/tlgbot/tlg_keywords_follower/venv/bin"
ExecStart=/home/tlgbot/tlg_keywords_follower/venv/bin/python bot.py
Restart=always
RestartSec=10

# Логирование
StandardOutput=append:/home/tlgbot/tlg_keywords_follower/bot.log
StandardError=append:/home/tlgbot/tlg_keywords_follower/bot_error.log

[Install]
WantedBy=multi-user.target
```

**Активация и запуск:**

```bash
# Перезагрузка systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable tlgbot.service

# Запустить сервис
sudo systemctl start tlgbot.service

# Проверить статус
sudo systemctl status tlgbot.service

# Просмотр логов
sudo journalctl -u tlgbot.service -f
# или
tail -f ~/tlg_keywords_follower/bot.log
```

#### 6.4. Управление сервисом

```bash
# Остановить бота
sudo systemctl stop tlgbot.service

# Перезапустить бота
sudo systemctl restart tlgbot.service

# Отключить автозапуск
sudo systemctl disable tlgbot.service

# Просмотр статуса
sudo systemctl status tlgbot.service
```

---

### Этап 7: Мониторинг и обслуживание (ongoing)

#### 7.1. Проверка работоспособности

```bash
# Статус сервиса
sudo systemctl status tlgbot.service

# Логи в реальном времени
sudo journalctl -u tlgbot.service -f

# Или файловые логи
tail -f ~/tlg_keywords_follower/bot.log
tail -f ~/tlg_keywords_follower/bot_error.log

# Проверка процесса
ps aux | grep bot.py
```

#### 7.2. Обновление конфигурации

```bash
# Редактировать config.json
nano ~/tlg_keywords_follower/config.json

# Добавить новый чат в monitored_chats
# Добавить новые ключевые слова в keywords

# Перезапустить сервис для применения изменений
sudo systemctl restart tlgbot.service
```

#### 7.3. Ротация логов

**Создать конфиг logrotate:**

```bash
sudo nano /etc/logrotate.d/tlgbot
```

**Содержимое:**

```
/home/tlgbot/tlg_keywords_follower/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 644 tlgbot tlgbot
}
```

#### 7.4. Backup важных файлов

**Простой скрипт бэкапа:**

```bash
#!/bin/bash
# ~/tlg_keywords_follower/backup.sh

BACKUP_DIR="$HOME/backups"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR

# Бэкап session файла (критично!)
cp ~/tlg_keywords_follower/*.session $BACKUP_DIR/session_$DATE.session 2>/dev/null

# Бэкап конфигурации
cp ~/tlg_keywords_follower/config.json $BACKUP_DIR/config_$DATE.json
cp ~/tlg_keywords_follower/.env $BACKUP_DIR/env_$DATE.txt

# Удаление старых бэкапов (>30 дней)
find $BACKUP_DIR -name "*.session" -mtime +30 -delete
find $BACKUP_DIR -name "*.json" -mtime +30 -delete
find $BACKUP_DIR -name "*.txt" -mtime +30 -delete

echo "Backup completed: $DATE"
```

**Добавить в crontab (ежедневный бэкап):**

```bash
crontab -e

# Добавить строку:
0 3 * * * /home/tlgbot/tlg_keywords_follower/backup.sh >> /home/tlgbot/tlg_keywords_follower/backup.log 2>&1
```

---

## Безопасность

### Критически важные аспекты

> [!CAUTION]
> **Session файл** - это полный доступ к вашему Telegram аккаунту!

#### Меры безопасности

**1. Session файл**

- Хранить только на защищённом сервере
- Никогда не коммитить в Git
- Регулярный backup в защищённое место
- Права доступа: `chmod 600 *.session`

**2. API credentials (.env файл)**

- Хранить в `.env` файле
- Не хардкодить в коде
- Права доступа: `chmod 600 .env`
- Не коммитить в Git (добавить в .gitignore)

**3. Сервер**

- Firewall (закрыть все порты кроме SSH)
- SSH доступ только по ключу (отключить пароли)
- Регулярные обновления: `sudo apt update && sudo apt upgrade`
- Использовать отдельного пользователя (не root)
- Включить fail2ban для защиты от брутфорса

**4. Ограничения**

- Не использовать основной аккаунт для userbot
- Создать отдельный Telegram аккаунт для бота
- Не давать боту излишние права

---

## Зависимости проекта (requirements.txt)

```txt
# Минимально необходимые библиотеки
telethon==1.34.0
python-dotenv==1.0.0
```

**Всего 2 зависимости!** Это и есть минимализм.

---

## Оценка времени разработки

| Этап | Время |
|------|-------|
| 1. Подготовка и проектирование | 1 день |
| 2. Настройка проекта | 0.5 дня |
| 3. Получение API credentials | 0.5 дня |
| 4. Разработка бота | 2-3 дня |
| 5. Тестирование | 1-2 дня |
| 6. Деплой на сервер | 1 день |
| 7. Документация и отладка | 0.5 дня |
| **ИТОГО** | **6-9 дней** |

> [!NOTE]
> Значительно быстрее чем с Docker и БД! Минимализм работает.

---

## Следующие шаги

1. ✅ Зарегистрировать приложение на <https://my.telegram.org>
2. ✅ Получить API_ID и API_HASH
3. ✅ Создать структуру проекта (bot.py, config.json, .env)
4. ✅ Установить зависимости: `pip install telethon python-dotenv`
5. ✅ Написать минимальный рабочий прототип
6. ✅ Протестировать локально
7. ✅ Развернуть на сервер через systemd

---

## Полезные ссылки

- [Telethon документация](https://docs.telethon.dev/)
- [Telethon примеры](https://docs.telethon.dev/en/stable/quick-references/faq.html)
- [Telegram API](https://core.telegram.org/api)
- [My Telegram Apps](https://my.telegram.org/apps)
- [Systemd tutorial](https://www.digitalocean.com/community/tutorials/how-to-use-systemctl-to-manage-systemd-services-and-units)

---

## Резюме подхода

✅ **Минимализм достигнут:**

- Один файл `bot.py` (~300-350 строк кода)
- JSON конфигурация вместо БД
- **Файловая очередь** вместо БД для персистентности
- Systemd вместо Docker
- Всего 2 библиотеки
- Простое развёртывание
- Легко поддерживать

✅ **Умные функции:**

- **Система правил (rules)** - разные ключевые слова → разные целевые группы
- Гибкая маршрутизация - одно сообщение может попадать под несколько правил
- Автоматическое получение chat_id при добавлении в группу
- Защита от зацикливания (все target_chat_ids из всех правил никогда не мониторятся)
- Пересылка в несколько целевых групп одновременно (с дедупликацией)
- Автоматическая валидация конфигурации при запуске
- **Файловая очередь** для надёжной доставки
- **Обработка FloodWaitError** - автоматический retry
- Персистентность очереди - сообщения не теряются при перезапуске
- Case-sensitive поиск на уровне каждого правила

❌ **Что исключили (по требованию):**

- Базы данных (SQLite/PostgreSQL)
- История сообщений
- Docker/Docker Compose
- Сложная модульная структура
- Расширенные команды управления
- Дополнительные зависимости

---

**Дата создания:** 2026-01-24  
**Версия:** 2.3 (Минимализм + Автоматизация + Очередь + Правила)  
**Изменения:**

- v2.0: Полная переработка под минималистичную архитектуру без БД и Docker
- v2.1: Добавлено автоматическое получение chat_id и защита от зацикливания
- v2.2: Добавлена файловая очередь и обработка FloodWaitError для надёжной доставки
- v2.3: Введена система правил - разные группы ключевых слов маршрутизируются в разные чаты
