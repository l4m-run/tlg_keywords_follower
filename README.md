# tlg_keywords_follower - Минималистичный бот для мониторинга чатов

Минималистичный Telegram UserBot для мониторинга чатов и пересылки сообщений по ключевым словам в целевые группы.

## Особенности

- 🎯 **Гибкая маршрутизация** - система правил с множественными целевыми группами
- 📝 **Простая настройка** - конфигурация через `config.json` и `rules.txt`
- 🔄 **Надёжная доставка** - файловая очередь с обработкой FloodWaitError
- 💬 **Управление через Telegram** - команды для управления правилами
- 🛡️ **Защита от зацикливания** - автоматическая проверка
- 📦 **Минимализм** - всего 5 Python файлов, без БД, без Docker

## Требования

- Python 3.11+
- Telegram аккаунт
- API credentials от Telegram

## Установка

### 1. Клонирование и установка зависимостей

```bash
python3 -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или .venv\Scripts\activate  # Windows
pip install -r requirements.txt

# Для запуска тестов:
pip install -r requirements.dev.txt
```

### 2. Получение API credentials

1. Зайти на <https://my.telegram.org>
2. Перейти в раздел "API development tools"
3. Создать приложение
4. Получить `API_ID` и `API_HASH`

### 3. Настройка

Создайте файл `.env`:

```bash
cp config.example.json config.json
nano config.json  # Опционально, если хотите изменить настройки
cp .env.example .env
nano .env  # Заполните своими данными
```

Пример `.env`:

```
API_ID=12345678
API_HASH=your_api_hash_here
PHONE=+79991234567
```

### 4. Настройка правил

Создайте `rules.txt` из примера:

```bash
cp rules.example.txt rules.txt
nano rules.txt
```

Формат `rules.txt`:

```
# Формат: название: ключевые_слова -> chat_id "Название группы"

urgent: срочно, важно, breaking -> -1001234567890 "Важные новости"
trading: deal, price, buy -> -1005555555555 "Трейдинг сигналы"
```

Как получить chat_id:

- Добавьте бота в группу - ID автоматически добавится в `monitored_chats`
- Используйте команду `/monitored_chats` чтобы увидеть все ID
- Скопируйте нужный ID в `rules.txt`

## Запуск

### Первый запуск (авторизация)

```bash
python -m app.main
```

Бот запросит:

1. Код подтверждения из Telegram
2. Пароль 2FA (если установлен)

После авторизации создастся session файл.

### Последующие запуски

```bash
python -m app.main
```

## Структура проекта

```
.
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── ...
├── config.json
├── rules.txt
├── .env
└── ...
```

## Использование

### Автоматическое добавление чатов

При `auto_add_chats: true` в `config.json`:

1. Добавьте бота в группу/канал
2. Chat ID автоматически добавится в `monitored_chats`
3. Бот начнёт мониторить этот чат

### Управление правилами через Telegram

Отправляйте команды в **Saved Messages (Избранное)**:

#### Просмотр

```
/rules                    # Список всех правил
/monitored_chats          # Список мониторимых чатов
/test срочная новость     # Проверить какие правила сработают
```

#### Добавление

```
/add_rule urgent срочно,важно -> -1001234
/add_keywords urgent breaking
/add_group urgent -1005678
```

#### Удаление

```
/remove_rule старое_правило
/remove_keywords urgent breaking
/remove_group urgent -1005678
```

#### Системные

```
/reload     # Перечитать rules.txt
/save       # Сохранить изменения
/validate   # Проверить корректность
/help       # Справка
```

Полный список команд в файле `commands_reference.md`.

## Конфигурация

### config.json

```json
{
  "monitored_chats": [
    {"id": -1001111111111, "name": "Новостной канал"},
    {"id": -1002222222222, "name": "Трейдинг сигналы"}
  ],
  "rules": [],
  "forward_mode": "copy",
  "auto_add_chats": true
}
```

- `monitored_chats` - откуда читать сообщения (автоматически пополняется)
- `rules` - правила фильтрации (управляются через rules.txt)
- `forward_mode` - `"forward"` (с автором) или `"copy"` (без автора)
- `auto_add_chats` - автоматически добавлять чаты при добавлении бота

### rules.txt

```
# Формат: название: ключевые_слова -> chat_id "Название" [case:on]

urgent: срочно, важно, breaking -> -1001234 "Важные", -1005678 "Резерв"
trading: deal, price -> -1009999 "Трейдинг" [case:on]
```

Одно сообщение может попасть под несколько правил и быть переслано во все указанные чаты.

## Деплой

### Вариант 1: Docker (Рекомендуется)

Самый простой способ запуска. Требует установленного Docker и Docker Compose.

1. Создайте `.env` и настройте конфиги (см. раздел Настройка).
2. Запустите скрипт деплоя:

    ```bash
    ./deploy_docker.sh
    ```

    Скрипт сам соберет образ, поможет с первой авторизацией и запустит бота в фоне.

### Вариант 2: Systemd (Linux)

Используйте скрипт `deploy.sh`. Он автоматически:

- Создаст виртуальное окружение `.venv`
- Установит зависимости
- Настроит systemd сервис `tlg_keywords_follower`
- Запустит бота

```bash
./deploy.sh
```

### Ручная установка

### 1. Копирование на сервер

```bash
scp -r tlg_keywords_follower/ user@server:/home/user/
```

### 2. Создание systemd сервиса

```bash
sudo nano /etc/systemd/system/tlg_keywords_follower.service
```

Содержимое:

```ini
[Unit]
Description=Telegram Keywords Follower Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/home/your_user/tlg_keywords_follower
Environment="PATH=/home/your_user/tlg_keywords_follower/.venv/bin"
ExecStart=/home/your_user/tlg_keywords_follower/.venv/bin/python -m app.main
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### 3. Запуск

```bash
sudo systemctl daemon-reload
sudo systemctl enable tlg_keywords_follower
sudo systemctl start tlg_keywords_follower
sudo systemctl status tlg_keywords_follower
```

### 4. Просмотр логов

```bash
sudo journalctl -u tlg_keywords_follower -f
# или
tail -f bot.log
```

## Безопасность

⚠️ **Важно:**

- `.env` и `*.session` файлы содержат полный доступ к аккаунту
- Никогда не коммитьте их в Git
- Установите права доступа: `chmod 600 .env *.session`
- Регулярно делайте backup `config.json` и `rules.txt`

## Troubleshooting

### FloodWaitError

Бот автоматически обрабатывает rate limits. Сообщения сохраняются в очереди и отправляются с задержкой.

### Бот не видит сообщения

Проверьте что chat_id в `monitored_chats`. Используйте `/monitored_chats`.

### Правило не срабатывает

Используйте `/test <текст>` чтобы проверить какие правила сработают.

### Зацикливание

Бот автоматически удаляет целевые чаты из мониторимых при старте.

## Документация

USER_GUIDE.md

## Лицензия

Freeware, AsIs
